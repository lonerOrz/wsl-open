[package]
name = "wsl-open"
version = "0.1.1"
edition = "2024"
description = "Low-level utility to open anything from WSL in Windows"
license = "MIT"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
